import React from "react";
import Choose from "./Choose";



const Start = ({props}) => {
  return (
    <div className=''>
      <Choose props={props} />
      
    </div>
  );
};

export default Start;
